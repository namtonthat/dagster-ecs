# Dagster Workspace Configuration for Production
#
# Dynamic multi-repository DAG loading from S3
# Automatically discovers all DAG files in /app/dags/
#
# Teams can deploy DAGs to s3://bucket/dags/team-name/
# and they will be automatically discovered and loaded

load_from:
  - python_module: dagster.workspace_loader